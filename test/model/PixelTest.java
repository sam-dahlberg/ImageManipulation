package model;

//import static org.junit.Assert.assertEquals;

import model.Pixel;
//import org.junit.Test;


/**
 * Test for Pixel.
 */
/*
public class PixelTest {

  Pixel validPixel;
  Pixel invalidPixelRedLow;
  Pixel invalidPixelRedHigh;
  Pixel invalidPixelGreenLow;
  Pixel invalidPixelGreenHigh;
  Pixel invalidPixelBlueLow;
  Pixel invalidPixelBlueHigh;
  Pixel invalidPixelMaxLow;


  @Test
  public void testValidPixel() {
    validPixel = new Pixel(130, 150, 60, 255);
    assertEquals(130, validPixel.getRed());
  }

  @Test(expected = IllegalArgumentException.class)
  public void invalidPixelRedLow() {
    invalidPixelRedLow = new Pixel(-1, 150, 60, 255);
  }

  @Test(expected = IllegalArgumentException.class)
  public void invalidPixelRedHigh() {
    invalidPixelRedHigh = new Pixel(300, 150, 60, 255);
  }

  @Test(expected = IllegalArgumentException.class)
  public void invalidPixelGreenLow() {
    invalidPixelGreenLow = new Pixel(150, -1, 60, 255);
  }

  @Test(expected = IllegalArgumentException.class)
  public void invalidPixelGreenHigh() {
    invalidPixelGreenHigh = new Pixel(150, 300, 60, 255);
  }

  @Test(expected = IllegalArgumentException.class)
  public void invalidPixelBlueLow() {
    invalidPixelBlueLow = new Pixel(150, 150, -1, 255);
  }

  @Test(expected = IllegalArgumentException.class)
  public void invalidPixelBlueHigh() {
    invalidPixelBlueHigh = new Pixel(150, 150, 300, 255);
  }

  @Test(expected = IllegalArgumentException.class)
  public void testInvalidPixelMaxLow() {
    invalidPixelMaxLow = new Pixel(150, 150, 150, -1);


 */
